<Page x:Class="ExpressTrack.ExpressDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpressTrack"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="880"
      Title="ExpressDetail">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Canvas Grid.Column="1" Background="#FFEAE1E1" Width="450" Margin="15,0">
            <Path Stroke="Black">
                <Path.Data>
                    <PathGeometry x:Name="trackGeometry" />
                </Path.Data>
            </Path>
        </Canvas>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="50,10">
            <Button x:Name="btnStart" Content="开始" Click="btnStart_Click" />
            <Button x:Name="btnAddPoint" Content="添加坐标" Margin="30,0,0,0" Click="btnAddPoint_Click" IsEnabled="False" />
            <Button x:Name="btnResetPoint" Content="重置" Margin="30,0,0,0" Click="btnResetPoint_Click" IsEnabled="False" />
        </StackPanel>
    </Grid>
</Page>
